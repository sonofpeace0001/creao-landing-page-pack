Research the top 5 premium SaaS landing pages in the Documentation space (Linear, Vercel, Framer, Raycast, Arc grade).
Break down what makes them feel premium: hero hook, whitespace, motion, social proof, CTA.
Then build me a complete, production-ready single-page landing page for Specwiki, API documentation made beautiful.

Audience: developer tooling teams.
Hero headline: "Docs developers will read."
Subheadline: "Specwiki turns your API into clean, interactive docs with live request examples and versioning."
Primary CTA: "Start free". Secondary CTA: "See an example".

Design system:
- Dark theme, background #0A0A0A, surfaces #141414
- Accent gradient from #0EA5E9 to #22D3EE
- Body font: Inter (Google Fonts). Headline font: Satoshi for elegance
- Generous whitespace, 120px section padding, 1200px max width
- Faint noise texture overlay + a slow animated mesh gradient behind the hero
- Glassmorphism sticky nav, subtle grain, 1px hairline borders at 8% white (dark) or 8% black (light)
- split-hero layout

Sections:
1. Sticky glass nav with "Specwiki" wordmark + 3 nav links + primary CTA
2. split-hero: headline on one side, floating product UI mockup (built in CSS only, no real image) on the other, with a subtle animated micro-interaction
3. Logo wall: Mintlify, ReadMe, Stoplight, Redocly, Sphinx, Docusaurus in muted monochrome
4. Bento grid: 4 feature cards with custom inline SVG line icons (no emoji, no stock):
   - Interactive examples (custom inline SVG line icon, no emoji, no stock)
   - Versioning (custom inline SVG line icon, no emoji, no stock)
   - Search (custom inline SVG line icon, no emoji, no stock)
   - AI assistant (custom inline SVG line icon, no emoji, no stock)
5. Metrics strip: 9k teams | 4.9 rating | 1.8M readers
6. Testimonial card: "Our support tickets dropped 30%. - Devon Park, DevRel" with a generated SVG monogram avatar
7. Pricing:       - Free
      - Pro $40
      - Enterprise custom with the middle tier highlighted (gradient border + "Most popular" badge) and a monthly/annual toggle that updates prices with JS (annual = 2 months free)
8. Final CTA band with gradient glow
9. Minimal footer

Motion:
- Scroll-reveal fade-in-up on each section via IntersectionObserver
- Hover glow + lift on cards and CTA buttons
- Smooth animated gradient on the hero mesh background
- Respect prefers-reduced-motion

Rules:
- No emoji icons, no stock illustrations, no stock photos, no generic AI-slop styling
- Mobile responsive with a sticky mobile CTA bar
- Single-file HTML with all CSS and JS inlined (Google Fonts via link is fine)
- Production-grade, editorial, refined. Looks like an 8k-dollar agency build.
- Use plain text, real layout. No lorem ipsum. Match the niche's tone.