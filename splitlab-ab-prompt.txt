Research the top 5 premium SaaS landing pages in the A/B testing space (Linear, Vercel, Framer, Raycast, Arc grade).
Break down what makes them feel premium: hero hook, whitespace, motion, social proof, CTA.
Then build me a complete, production-ready single-page landing page for Splitlab, A/B testing for marketers.

Audience: marketing teams optimizing funnels.
Hero headline: "Test. Learn. Convert."
Subheadline: "Splitlab runs experiments on your pages, shows statistically significant winners, and auto-promotes them."
Primary CTA: "Start free". Secondary CTA: "See a test".

Design system:
- Light theme, off-white #FAFAF7, surfaces #FFFFFF, soft #F4F4F2 cards
- Accent gradient from #7C3AED to #EC4899
- Body font: Inter (Google Fonts). Headline font: Satoshi for elegance
- Generous whitespace, 120px section padding, 1200px max width
- Faint noise texture overlay + a slow animated mesh gradient behind the hero
- Glassmorphism sticky nav, subtle grain, 1px hairline borders at 8% white (dark) or 8% black (light)
- centered-hero layout

Sections:
1. Sticky glass nav with "Splitlab" wordmark + 3 nav links + primary CTA
2. centered-hero: headline on one side, floating product UI mockup (built in CSS only, no real image) on the other, with a subtle animated micro-interaction
3. Logo wall: Optimizely, VWO, AB Tasty, Convert, GrowthBook, PostHog in muted monochrome
4. Bento grid: 4 feature cards with custom inline SVG line icons (no emoji, no stock):
   - Visual editor (custom inline SVG line icon, no emoji, no stock)
   - Stats engine (custom inline SVG line icon, no emoji, no stock)
   - Auto-promote (custom inline SVG line icon, no emoji, no stock)
   - Audience targeting (custom inline SVG line icon, no emoji, no stock)
5. Metrics strip: 12k teams | 4.8 rating | 2.1M experiments
6. Testimonial card: "We found a 23% lift in one week. - Elise Carter, Growth" with a generated SVG monogram avatar
7. Pricing:       - Free
      - Growth $49
      - Pro $149 with the middle tier highlighted (gradient border + "Most popular" badge) and a monthly/annual toggle that updates prices with JS (annual = 2 months free)
8. Final CTA band with gradient glow
9. Minimal footer

Motion:
- Scroll-reveal fade-in-up on each section via IntersectionObserver
- Hover glow + lift on cards and CTA buttons
- Smooth animated gradient on the hero mesh background
- Respect prefers-reduced-motion

Rules:
- No emoji icons, no stock illustrations, no stock photos, no generic AI-slop styling
- Mobile responsive with a sticky mobile CTA bar
- Single-file HTML with all CSS and JS inlined (Google Fonts via link is fine)
- Production-grade, editorial, refined. Looks like an 8k-dollar agency build.
- Use plain text, real layout. No lorem ipsum. Match the niche's tone.