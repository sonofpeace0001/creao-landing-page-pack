Research the top 5 premium SaaS landing pages in the E-commerce space (Linear, Vercel, Framer, Raycast, Arc grade).
Break down what makes them feel premium: hero hook, whitespace, motion, social proof, CTA.
Then build me a complete, production-ready single-page landing page for ShopMe, store builder for creators.

Audience: creators selling physical products.
Hero headline: "Launch your store in an afternoon."
Subheadline: "ShopMe gives you a storefront, payments, and shipping tools with no code and no fees on your first 100 orders."
Primary CTA: "Start free". Secondary CTA: "See a store".

Design system:
- Light theme, off-white #FAFAF7, surfaces #FFFFFF, soft #F4F4F2 cards
- Accent gradient from #111827 to #6366F1
- Body font: Inter (Google Fonts). Headline font: Satoshi for elegance
- Generous whitespace, 120px section padding, 1200px max width
- Faint noise texture overlay + a slow animated mesh gradient behind the hero
- Glassmorphism sticky nav, subtle grain, 1px hairline borders at 8% white (dark) or 8% black (light)
- asymmetric-hero layout

Sections:
1. Sticky glass nav with "ShopMe" wordmark + 3 nav links + primary CTA
2. asymmetric-hero: headline on one side, floating product UI mockup (built in CSS only, no real image) on the other, with a subtle animated micro-interaction
3. Logo wall: Shopify, BigCartel, Gumroad, Stan, Lemon Squeezy, Payhip in muted monochrome
4. Bento grid: 4 feature cards with custom inline SVG line icons (no emoji, no stock):
   - No-code storefront (custom inline SVG line icon, no emoji, no stock)
   - Instant payouts (custom inline SVG line icon, no emoji, no stock)
   - Shipping labels (custom inline SVG line icon, no emoji, no stock)
   - Inventory sync (custom inline SVG line icon, no emoji, no stock)
5. Metrics strip: 14k stores | $28M sold | 4.8 rating
6. Testimonial card: "I made my first sale the same day. - Marcus Reed, Maker" with a generated SVG monogram avatar
7. Pricing:       - Free
      - Standard $29
      - Pro $79 with the middle tier highlighted (gradient border + "Most popular" badge) and a monthly/annual toggle that updates prices with JS (annual = 2 months free)
8. Final CTA band with gradient glow
9. Minimal footer

Motion:
- Scroll-reveal fade-in-up on each section via IntersectionObserver
- Hover glow + lift on cards and CTA buttons
- Smooth animated gradient on the hero mesh background
- Respect prefers-reduced-motion

Rules:
- No emoji icons, no stock illustrations, no stock photos, no generic AI-slop styling
- Mobile responsive with a sticky mobile CTA bar
- Single-file HTML with all CSS and JS inlined (Google Fonts via link is fine)
- Production-grade, editorial, refined. Looks like an 8k-dollar agency build.
- Use plain text, real layout. No lorem ipsum. Match the niche's tone.